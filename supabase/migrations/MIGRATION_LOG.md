# Migration Log

Append an entry for every migration. Format: date | filename | what changed | why.

2026-05-15 | 20260515120000_google_oauth_tokens.sql | Create google_oauth_tokens table | Store Google Calendar refresh + access tokens for Tab 2 and Tab 3.
2026-05-16 | 20260516120000_briefings.sql | Create briefings table (id, created_at, date, kind, summary, model, input_hash, expires_at) with date index and unique (date, kind) index | Cache daily / weekly AI Digest briefings for Tab 2 so we don't re-run unchanged prompts; unique constraint enforces at most one cached briefing per (date, kind).
2026-05-17 | 20260517120000_briefings_append_only.sql | Drop unique index `briefings_date_kind_unique_idx`; add non-unique btree index `briefings_date_kind_created_at_idx` on (date, kind, created_at desc) | Switch briefings to append-only history so every regeneration preserves the prior row; "current" briefing is the most recent row per (date, kind). Markus explicitly confirmed the index drop in chat — no data is lost (the constraint is removed, rows remain).
2026-05-18 | 20260518120000_time_block_suggestions.sql | Create time_block_suggestions table (id, created_at, date, project_name, start_at, end_at, rationale, status check ('pending','confirmed','dismissed'), google_event_id, batch_id) with indexes on (date) and (date, status, created_at desc) | Persist Sonnet-proposed time blocks for Tab 2; append-only history (old confirmed/dismissed rows stay for future history viewer), UI surfaces only today's pending rows ordered by start_at; batch_id groups suggestions generated in one click.
2026-05-19 | 20260519120000_user_settings.sql | Create user_settings table (user_key pk, timezone default 'Asia/Dubai', master_calendar_id nullable, task_type_windows jsonb default '[]', updated_at) and seed the 'markus' row with defaults via ON CONFLICT DO NOTHING | Persist user-configurable timezone, master Google calendar, and per-task-type working-hour windows for Tab 2 settings; idempotent seed so re-running the migration on a fresh DB is safe. Wired into /api/profile/settings in this prompt; time-block + briefings routes still use hardcoded Berlin/09-18 until a follow-up prompt threads settings through.
2026-05-18 | 20260520120000_client_template_overrides.sql | Create client_template_overrides table (id uuid pk, zoho_contact_id, template_key, custom_text, created_at, updated_at; UNIQUE (zoho_contact_id, template_key); index on zoho_contact_id) | Persist per-client overrides for the four WhatsApp message templates on Tab 4. Default (German) text lives in constants/translations.ts; this table only stores rows where Markus has customised the wording for a specific client. Filename uses the 20260520 slot because the 20260518 slot was already taken by time_block_suggestions — order in this log reflects creation date, not the timestamp prefix.
2026-06-07 | 20260607120000_briefings_locale.sql | Add `locale text not null default 'de'` to briefings | Make the daily AI Digest (Tab 2) locale-aware: the briefing is now generated in the selected language (DE default, section titles included) and cached separately per locale. The route keys cache lookups on (date, kind, locale) so DE and EN are distinct cache entries and toggling reuses each language's cache instead of re-running Sonnet. Additive, no data loss — pre-existing (English) rows keep their content; first request per locale generates a locale-correct briefing.
2026-06-13 | 20260613120000_user_settings_profile_prefs.sql | Add four columns to user_settings: `timeblock_horizon_days int not null default 5`, `workday_start_hour int not null default 9`, `workday_end_hour int not null default 18`, `default_tab text not null default '/projects'` (all `add column if not exists`) | Persist three new profile preferences for Tab 2 / root redirect: the time-block planner horizon (1–7 days, was hardcoded `DEFAULT_HORIZON_DAYS=5`), the global default workday window (was hardcoded 09–18, used as fallback when no task_type_windows are configured), and the default landing tab for `/` (was a hardcoded redirect to /projects). Additive, no data loss — the existing 'markus' row picks up the column defaults automatically so behaviour is unchanged until Markus edits the values in /profile.
